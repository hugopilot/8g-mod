import discord

elevated = ['Admin', 'Moderators']


def has_elevated_perms(user: discord.Member):
    """Check if user has elevated permissions
    
        Parameters:
        user: User to check if he has elevated permissions
    """
    return False
